app = 'skeptic-bot-staging'
primary_region = 'fra'
kill_signal = 'SIGTERM'

[build]

[deploy]
strategy = 'immediate'
release_command = '/app/bin/migrate'

[env]
PHX_HOST = 'skeptic-bot-staging.fly.dev'
PORT = '8080'
PRIMARY_REGION = 'fra'

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = 'stop'
auto_start_machines = true
min_machines_running = 0
processes = ['app']

[http_service.concurrency]
type = 'connections'
hard_limit = 1000
soft_limit = 1000

[[http_service.checks]]
interval = '30s'
timeout = '5s'
grace_period = '10s'
method = 'GET'
path = '/health'

[[vm]]
memory = '512mb'
cpu_kind = 'shared'
cpus = 1
